<div>

<header>
<h1>Words</h1>
</header>
<div>&nbsp;</div>
<table class="table table-striped">
	<thead>
	 <th>Words</th>
	 <th>Letters</th>
	</thead>
	<tbody>
	 <% @words.each do |word| %>
	  <tr>
	  	<td>
	  	<a href="<%= "/anagrams/#{word.text}" %>">
	  	<%= "#{word.text}" %></a></td>
	  	<td><%= "#{word.letters}" %></td>
	  </tr>
	<% end %>
	</tbody>
</table>

</div>