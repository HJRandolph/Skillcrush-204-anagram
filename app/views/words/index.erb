<div>

<header>
<h1>Words</h1>
</header>
<div><a href="/">Back to the generator!</a></div>
<table class="table table-striped">
	<thead>
	 <th>Words</th>
	 <th>Letters</th>
	</thead>
	<tbody>
	 <% @word.each do |word| %>
	  <tr>
	  	<td>
	  	<a href="<%= "/anagrams/#{word.text}" %>">
	  	<%= "#{word.text}" %></a></td>
	  	<td><%= "#{word.letters}" %></td>
	  </tr>
	<% end %>
	</tbody>
</table>



</div>