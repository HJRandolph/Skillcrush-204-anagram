<div>

<header>
<h1>Dictionary</h1>
</header>
<div><a href="/">Back to the generator!</a></div>
<table class="table table-striped">
	<thead>
	 <th>Words</th>
	 <th>Letters</th>
	 <th colspan="2">Actions</th>
	</thead>
	<tbody>
	 <% @word.each do |word| %>
	  <tr>
	  	<td>
	  	<a href="<%= "/dictionary/#{word.id}" %>">
	  	<%= "#{word.text}" %></a></td>
	  	<td><%= "#{word.letters}" %></td>
	  	<td><a href="<%= "/dictionary/#{word.id}/edit" %>">Edit</a></td>
	  	<td>
	  	<form action="<%= "/dictionary/#{word.id}" %>" method="post">
	  	<input type="hidden" name="_method" value="delete" />
	  	<button class="btn-danger" type="submit">Delete</button>
	  	</form>
	  	</td>
	  </tr>
	<% end %>
	</tbody>
</table>



</div>